# @package _global_
defaults:
  - /experiment/wt103/gpt2.yaml
  - override /model: gpt2

#mode: eval

#eval:
#  checkpoint_type: pytorch
#  ckpt: /home/ks152/Experiments/logs/runs/2024-04-26/13-46-06/checkpoints/None/last.ckpt/checkpoint/mp_rank_00_model_states.pt
#  run_val: False  # val and test are the same for ImageNet
#  run_test: True
